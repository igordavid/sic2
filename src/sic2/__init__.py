# __init__.py — marks this directory as a Python *package*.
#
# When Python sees a folder containing __init__.py it treats that folder
# as a module that can be imported with `import sic2`.
#
# This file is intentionally minimal: it just provides a short description
# string that tools like `help(sic2)` will display.
"""sic2 — secret scanner for source code repositories."""
